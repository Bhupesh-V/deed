{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Deed",
            "type": "shell",
            "command": "go build -gcflags='all=-N -l' -o deed",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$go"
            ]
        }
    ]
}