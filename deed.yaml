version: "1"

database:
  name: postgres

global:
  ignore_tables:
    - migrations

rules:
  tables:
    users:
      count: 100
      columns:
        username:
          type: regex
          pattern: "^[a-z0-9_]{3,16}$"

      # status:
      #   type: enum
      #   values: ["active", "suspended", "pending"]

      # age:
      #   type: range
      #   min: 18
      #   max: 65

      # created_at:
      #   type: datetime
      #   start: "2023-01-01"
      #   end: "2026-12-31"
