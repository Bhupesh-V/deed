version: "1"

global:
  ignore_tables:
    - migrations

tables:
  users:
    count: 100
    columns:
      username:
        type: regex
        pattern: "^[a-z0-9_]{3,16}$"

      # status:
      #   type: enum
      #   values: ["active", "suspended", "pending"]

      # age:
      #   type: range
      #   min: 18
      #   max: 65

      # created_at:
      #   type: datetime
      #   start: "2023-01-01"
      #   end: "2026-12-31"

      bio:
        type: regex
        pattern: "[A-Za-z0-9 ]{20,50}"
        # nullable: true
        # null_percentage: 20
